var framework = "GiavaScript";
var major = 0;
var minor = 1;

var versionLine = `Welcome to ${framework} v${major}.${minor}`;
versionLine;

var items = ["parser", "runtime", "specs"];
`Shipped features: ${items}`;

var nested = `Nested output: ${`inner ${2 + 3}`}`;
nested;

`Escaped interpolation marker: \${notInterpolated}`;
